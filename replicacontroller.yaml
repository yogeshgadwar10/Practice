apiVersion: v1
kind: ReplicationController
metadata:
    name: my-rc
    labels:
      app: my-app
spec:
  template:
    metadata:
      name: my-rc
      labels:
         app: my-app

    spec:
      containers:
        - name: httpd-app
          image: httpd
          ports:
           - containerPort: 80

  replicas: 3
  selector:
    app: my-app

---
apiVersion: v1
kind: Service
metadata: 
  name: cloth-service
  labels:
    app: cloth-app
spec:
  ports:
   - targetPort: 80
     port: 80
     protocol: TCP
     name: http

  selector:
    app: my-cloth-service
  type: NodePort

